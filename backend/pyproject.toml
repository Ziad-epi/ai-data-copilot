[project]
name = "ai-data-copilot-backend"
version = "0.1.0"
description = "Backend API for ai-data-copilot."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "pydantic-settings>=2.2.1",
]

[tool.uvicorn]
factory = false
